services:
  # ─────────────────────────────────────────────
  # PicoClaw Agent (one-shot query)
  #   docker compose run --rm picoclaw-agent -m "Hello"
  # ─────────────────────────────────────────────
  picoclaw-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: picoclaw-agent
    profiles:
      - agent
    volumes:
      # Configuration file (mount your own config with real credentials)
      - ${PICOCLAW_CONFIG:-./config/config.json}:/root/.picoclaw/config.json:ro
      # Codex CLI auth directory
      - ~/.codex:/root/.codex:ro
      # Persistent workspace (sessions, memory, logs)
      - picoclaw-workspace:/root/.picoclaw/workspace
    entrypoint: ["picoclaw", "agent"]
    stdin_open: true
    tty: true

  # ─────────────────────────────────────────────
  # PicoClaw Gateway (Long-running Bot)
  #   docker compose up picoclaw-gateway
  # ─────────────────────────────────────────────
  picoclaw-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: picoclaw-gateway
    restart: unless-stopped
    profiles:
      - gateway
    volumes:
      # Configuration file (mount your own config with real credentials)
      # For development: use ./config/config.json (in repo, generic)
      # For deployment: use ~/.picoclaw/config.json (outside repo, with secrets)
      - ${PICOCLAW_CONFIG:-./config/config.json}:/root/.picoclaw/config.json:ro
      # Codex CLI auth directory
      - ~/.codex:/root/.codex:ro
      # Persistent workspace (sessions, memory, logs)
      - picoclaw-workspace:/root/.picoclaw/workspace
    command: ["gateway"]

volumes:
  picoclaw-workspace:
